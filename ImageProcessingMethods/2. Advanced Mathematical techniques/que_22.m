clc
clear all;
c=-1;d=-1;
x=-4:1:-1;y=-4:1:-1;
z=1:1:4;w=1:1:4;
r=sqrt((2*pi*c)./x);
f=1./r
E=(x+(w./(y+z)))./(x+(w./(y-z)))
A=(exp(-c./(2.*x)))./(log(y).*sqrt(d.*z))
S=(x.*(2.15+0.35*y).^1.8)./(z.*(1-x).^y)